# Salin ke .env, isi manual. Jangan pernah commit .env.
# Virtual key /v1/* dibikin lewat POST /admin/keys (pakai SANMON_ADMIN_TOKEN),
# bukan env var lagi.
GEMINI_API_KEY=
SANMON_ADMIN_TOKEN=
SANMON_DATABASE_URL=postgres://sanmon:sanmon@localhost:5435/sanmon?sslmode=disable
